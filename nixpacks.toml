[phases.setup]
nixPkgs = ["ffmpeg", "python311Full"]

[phases.install]
cmds = ["python3.11 -m ensurepip --upgrade && python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 sorcerer.py daemon"
